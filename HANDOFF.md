# HANDOFF — Tian OS MathPath Fractions Pilot

**For:** the next Claude Code session continuing this work.
**Updated:** 2026-06-10.
**Read these first (source of truth):** `docs/tian-os-current-brief.md` (§5 guardrails, §9–§11 verification),
`docs/mathpath/PILOT_FIX_PLAN.md` (§1 P0 blockers, §1b evidence/access), `NEXT.md`,
`docs/tian-os/SPRINT3_FRACTIONS_CONTENT_EVIDENCE_REPAIR.md` (C1/C2 detail).

---

## Where things stand

**Done & verified:**
- **C1 — fraction mapping quarantine: DONE, committed, verified.** 12 incorrect families quarantined in
  `frontend/src/mathpath/fractions/fractionQuestionFamilies.js` (re-derived against current source, not the
  stale 2026-06-07 audit). Verified under Node: 12 IDs correct, every skill F001–F026 keeps ≥1 active
  family (F018 lost two but retains 4: `QF_F018_001`–`004`), no quarantined family leaks into selection.
- **§1 P0 "demo-student" fallback removal: appears DONE** — only remaining reference is the guard test
  `frontend/src/pages/student/StudentPilotCopy.test.jsx`.
- Sprint 1 practice-integrity fixes (E1 accuracy denominator, U1 dedupe, U2 diagram serialization): done.

**Open — do these next, IN ORDER. Do NOT start new modules.**

1. **C2 — runtime evidence repair (needs live DB; founder runs, or run if DB is available).**
   ```
   node scripts/runFractionsRuntimeEvidenceRepair.js          # dry-run: review issue counts + claim gate
   node scripts/backfillLegacyMistakeEvidence.js --apply       # normalise legacy mistakes (no mastery inflation)
   node scripts/runFractionsRuntimeEvidenceRepair.js --apply   # persist recheck repairs + seed worksheet audit
   node scripts/runFractionsRuntimeEvidenceRepair.js           # re-run dry-run; confirm gate clean/expected-partial
   ```
   Done when: pilot-claim gate reads clean (or only expected "partial"); zero high-severity issues.

2. **Remaining §1 P0 runtime blockers** (`PILOT_FIX_PLAN.md` §1):
   - Diagnostic completes reliably end-to-end (`routes/diagnostics.js`, `services/diagnostics/*`,
     diagnostic screens). A 10-question diagnostic must not end after 1 answer.
   - Practice attempts persist for BOTH local-generated and API sessions, paths must not diverge
     (`fractionPracticeFlow.js`, `routes/practice.js`, `routes/mastery.js`, `models/PracticeAttempt.js`).
   - Verify each: relevant unit/backend test + `npm --prefix frontend run build`.

3. **§1b evidence/access consistency** (`PILOT_FIX_PLAN.md`):
   - Confirm `hasMasteryEvidence` (`services/mathpath/fractionsRuntimeEvidenceIntegrityService.js`)
     requires a passing recheck/retention; set Fractions `requiresMisconceptionResolved: true`; stop
     auto-inferring `mastery_check` from attempt count (`services/mathpath/masteryCriteriaEngine.js`).
   - Enforce `StudentGuardian.accessLevel === 'view_only'` on guardian WRITE routes
     (`routes/parentInvites.js` assigns it; no write route enforces it yet).

4. **Finish remaining Sprint 2** (U5 save-and-exit without completing the session; P4 open-working
   friction; P5 child-friendly review copy) per `docs/tian-os/CURRENT_PRACTICE_UX_AND_EVIDENCE_TRIAGE.md`.

5. **Final gate:** `npm --prefix frontend test`, `npm --prefix frontend run build`,
   `node scripts/qa-pilot-preflight.js`.

---

## STOP doing (scope drift)

Recent commits added **P1 (Primary-1) math runtime** (`feature/p1-math-runtime`) and **agency/Stripe-Connect
billing** ("Gap B"). These are **explicit non-goals** in `PILOT_FIX_PLAN.md` ("Explicit Non-Goals" + §5)
until the F001–F026 Fractions loop passes pilot QA. Pause them and return to the list above.

## Guardrails (do NOT break — `tian-os-current-brief.md` §5)

- Never weaken `StudentGuardian` access; always resolve students via `resolveStudent()`.
- Never remove evidence fields on `Mistake` / `PracticeAttempt` / working models (additive only).
- Never expose raw F-codes or misconception IDs in student/parent UI (use human labels).
- Never claim "mastered" / "P1–P6 ready" beyond the gated `mistakeCorrectionFlow` progression.
- Keep school-admin / teacher-dashboard / notifications / entitlements feature-flag gated.
- Don't remove/rewrite working-evidence components (WorkingCanvas, FullScreenWorkingMode,
  WorkingEvidenceDecision, drawingUtils, working/ upload screens). Refactors OK if exports preserved.
- Commit per task; run tests/build before checking anything off; no Playwright artifacts in commits.

## Repo notes

- Branches: work has been on `main`, `feature/tutor-explanation-recorder`, `feature/p1-math-runtime`.
  Decide a single working branch and stick to it. `main` has had several unpushed commits — back it up
  (`git push origin main:main`) when ready (may trigger a deploy).
- The sandbox cannot run vitest (platform-mismatched native binaries + blocked registry) or hit the DB.
  Run the full test suite / DB scripts on macOS.
- `TASKS.md` is auto-regenerated by the continuous runner — regenerate it FROM `PILOT_FIX_PLAN.md`
  (which now includes §1b) rather than overwriting those items.
